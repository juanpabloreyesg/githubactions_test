name: Hola mundo
on: push

jobs:
  Mi-primer-job:
    name: Mi primer job
    runs-on: ubuntu-latest
    outputs:
      output: ${steps.imprimir.respuesta}
    steps:
    - id: imprimir 
      env:
        MI_VARIABLE: Hola mundo
      run: echo "::set-output name=respuesta::hola"
  Mi-segundo-job:
    needs: Mi-primer-job
    name: Mi segundo job
    runs-on: ubuntu-latest
    steps:
    - id: Imprimir 
      run: echo ${{needs.Mi-primer-job.outputs.output}}

